# Region-specific API origin, without a trailing slash.
RIFTBOUND_API_BASE_URL=https://asia.api.riotgames.com

# Optional. Keep secrets in .env.local; never commit real keys.
RIFTBOUND_API_KEY=
RIFTBOUND_API_KEY_HEADER=X-Riot-Token
